import 'package:flutter/material.dart';

class guneyVSkore extends StatefulWidget {
  const guneyVSkore({Key? key}) : super(key: key);

  @override
  _guneyVSkoreState createState() => _guneyVSkoreState();
}

class _guneyVSkoreState extends State<guneyVSkore> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("G.Kore vs K.Kore"),
        centerTitle: true,
      ),
    );
  }
}
